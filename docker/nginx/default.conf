server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Try to serve static files directly, fall back to the SPA entrypoint
    location / {
        try_files $uri $uri/ /index.html;
    }
}
